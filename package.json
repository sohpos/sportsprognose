{
  "name": "sportsprognose",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/web",
    "backend"
  ],
  "scripts": {
    "dev:backend": "npm run dev --workspace=backend",
    "dev:web": "npm run dev --workspace=apps/web",
    "build": "npm run build --workspace=packages/core && npm run build --workspace=backend && npm run build --workspace=apps/web",
    "install:all": "npm install"
  }
}
